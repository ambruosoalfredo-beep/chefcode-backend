/**
 * ChefCode Mobile - Entry Point
 * React Native/Expo Entry Point
 */

import { registerRootComponent } from 'expo';
import App from './src/App';

// Registra il componente principale dell'app
registerRootComponent(App);