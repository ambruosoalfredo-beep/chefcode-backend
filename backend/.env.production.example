# ChefCode Backend - Production Environment Variables
# Queste variabili vanno configurate SOLO su Render.com
# NON inserire mai valori reali in questo file!

# OpenAI API Key (ottieni da https://platform.openai.com/api-keys)
OPENAI_API_KEY=your_openai_api_key_here

# Ambiente di produzione
NODE_ENV=production

# Domini consentiti per CORS (* per tutti, o lista separata da virgole)
ALLOWED_ORIGINS=*

# Porta (Render la configura automaticamente)
PORT=3000